include_rules

LIBS+= -pthread -l$(LIB_TSYN) -lboost_system
EXTRADEP+=
LIBPATH+= -L$(LD_PATH)
INCLUDE_PATH+= -I$(PHOME)/src

: foreach *.cpp |> !cpp |>
: server_*.o | $(LD_PATH)/$(LIB_TSYN_SO) |> !link_binary |> server_demo
: client_*.o | $(LD_PATH)/$(LIB_TSYN_SO) |> !link_binary |> client_demo

